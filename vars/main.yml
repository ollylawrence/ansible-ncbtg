# SPDX-License-Identifier: MIT-0
---
# vars file for ansible-ncbtg

git_commit_message: "Ansible automated backup commit at {{ now(utc=true, fmt='%Y-%m-%d %H:%M:%S') }} UTC"
git_user_name: "{{ ansible_user }}"
